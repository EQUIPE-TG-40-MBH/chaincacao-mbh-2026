import 'package:flutter/material.dart';

class EcranSaisieId extends StatelessWidget {
  const EcranSaisieId({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Saisie ID Manuelle")));
}